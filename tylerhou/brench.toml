benchmarks = '../benchmarks/core/*.bril'
extract = 'total_dyn_inst: (\d+)'

[runs.baseline]
pipeline = [
    "bril2json",
    "_build/install/default/bin/opt",
    "brili -p {args}",
]

# [runs.valnum-constprop-dce]
# pipeline = [
#     "bril2json",
#     "_build/install/default/bin/opt dce cp",
#     "brili -p {args}",
# ]

