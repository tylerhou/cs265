@main {
.__b1:
  a0: int = const 10;
  jmp .while.cond(a0);
.while.cond(a1: int):
  zero: int = const 0;
  is_term: bool = eq a1 zero;
  br is_term .while.finish(a1) .while.body(a1);
.while.body(a2: int):
  one: int = const 1;
  a3: int = sub a2 one;
  jmp .while.cond(a3);
.while.finish(a4: int):
  print a4;
  ret;
}
